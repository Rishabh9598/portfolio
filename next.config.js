/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'blog.socialchamp.com',
      'img.freepik.com',
      'images.unsplash.com',
      'static.wixstatic.com'
    ],
  },
};

module.exports = nextConfig; 